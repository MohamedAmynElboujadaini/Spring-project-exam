pipeline {
    agent any

    stages {
        stage("testing") {
            steps {
                script {
                    // Clean and build the Maven project
                    echo "First stage"
                }
            }
        }


    }

    
}
